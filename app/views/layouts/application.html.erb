<!DOCTYPE html>
<html>
  <head>
    <title>SecretShareApp</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <p class="flash"><%= flash.notice %></p>

    <div class="header">
      <div class="company_info">
        <h1 class="logo">
          <%= link_to "Pandora Boxx", root_path %>
        </h1>

        <p>Your secrets are safe with us</p>
      </div>

      <% if logged_in? %>
        <div class="user_info">
          <p> Hey there, <%= @current_user.screen_name %>!</p>
          <%= button_to "Logout", sessions_path, method: :delete %>
          <%= link_to "Edit Info", edit_user_path %>
        </div>
      <% end %>
    </div>

    <%= yield %>

  </body>
</html>
